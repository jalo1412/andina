<nav class="navbar navbar-default" role="navigation">
  <div data-turbolinks="false">

    <div class="navbar-header">
      <%= link_to "Andina", store_index_path, class: "navbar-brand", data: { no_turbolink: true } %>
    </div>
    <form class="navbar-form navbar-left" action="/action_page.php">
      <div class="input-group">
        <input type="text" class="form-control" placeholder="Suchen">
        <div class="input-group-btn">
          <button class="btn btn-default" type="submit">
            <i class="glyphicon glyphicon-search"></i>
          </button>
        </div>
      </div>
    </form>
    <ul class="#"></ul>
    <div id="nav-navbar-right">
      <ul class="nav navbar-nav navbar-right">
        <% if not session[:user_id] %>
          <li>
            <%=link_to new_user_path, class:'a' do%>
            <i class="glyphicon glyphicon-plus"></i>
            New Account
          <% end %>
        </li>
        <li>
          <%=link_to login_path, class:'a' do%>
          <i class="glyphicon glyphicon-user"></i>
          Logg In
        <% end %>
      </li>
    <% end %>
    <% if session[:user_id] %>
    <li>
      <%=link_to logout_path, method: :delete, id:'a-out' do%>
      <i class="glyphicon glyphicon-remove"></i>
      logg-out
    <% end %>
  </li>
      <li>
        <%=link_to @user, class:'a' do%>
        <i class="glyphicon glyphicon-user"></i>
        My Account
      <% end %>
    </li>
  <% end %>
  <li>
    <%= link_to @cart, class:'a' do %>
    <i class="glyphicon glyphicon-shopping-cart"></i>
    Einkaufswagen
    <% if @cart %>
      <% items = @cart.line_items.to_a.sum {|item| item.quantity} %>
        <% if items > 0 %>
          <!-- use <span class="badge badge-secondary badge-pill"> for more style-->
          <div id="shopping-circle"><%= items %></div>
        <% end %>
      <%end%>
    <%end%>
  </li>
  <li></li>
</ul>
</div>
</div>
</nav>
